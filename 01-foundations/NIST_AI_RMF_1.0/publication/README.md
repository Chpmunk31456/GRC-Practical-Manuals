# Manual 03 Publication Package

Manual 03 publication artifacts are generated by the controlled `10 - NIST AI RMF Manual 03 QA` workflow using `scripts/generate_nist_ai_rmf_manual_03_publication_v3.py`.

The release package is trilingual (`en`, `es-419`, `pt-BR`) and consists of one DOCX and one PDF per language, together with generated graphics, page renders/contact sheets, a publication report, page-QA evidence, and SHA-256 checksums.

Publication remains fail-closed until the repository contains the durable approved package and the required human semantic, rendered accessibility/visual, changed-scope, and Final Human Release Approval records are complete for the exact candidate.
