# Trigger CIS Portuguese opening repair

Run the controlled integration job against `production/multilingual-grc-editions`.

This PR exists only to generate an observable `pull_request` workflow event after connector-originated push events failed to start the job.

This request modifies no publication content on `main`.
