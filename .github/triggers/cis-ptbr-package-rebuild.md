# Trigger CIS Controls v8.1 PT-BR package rebuild

Normalize the ten resolvable local image tags, rebuild the Brazilian Portuguese DOCX/PDF package, validate all ten embedded image placements, render every PDF page, rerun the repository-wide embedded-media validator, generate checksums, and upload review evidence.

Trigger only. Do not merge this branch or pull request.
